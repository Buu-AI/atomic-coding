# Privy authentication
# Public app ID, exposed to the browser
NEXT_PUBLIC_PRIVY_APP_ID=
# Server-side app ID (mirrors NEXT_PUBLIC_PRIVY_APP_ID)
PRIVY_APP_ID=
# Server-side app secret, never exposed to the client
PRIVY_APP_SECRET=
